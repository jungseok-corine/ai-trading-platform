# M2.15B-6 — Daily Candle Execute Pilot (005930 only, dev DB)

> **첫 실제 execute-pilot.** 1종목(005930) 일봉 252봉을 KIS read-only로 받아 dev DB `market_data`
> `timeframe="1d"`에 멱등 적재. **5종목/20종목/110종목/전체 유니버스 미실행 · 마이그레이션 없음 ·
> 런타임 코드 변경 없음 · 프론트 없음 · SignalLog/Trade/Order 미생성 · KIS 주문 API/place_order 미호출 ·
> 스케줄러/디스패처 없음 · production DB 미접촉 · M2.14B-3d 미승인.**

---

## 1. Executive summary

**성공.** 005930 일봉 **252봉을 dev DB에 적재**(market_data `1d`: 0 → 252). 중복 0, OHLCV/volume 결측 0,
인트라데이(1m/5m) 보존, Trade 35 불변, SignalLog 본 작업 생성 0. 적재 후 coverage `ready_for_52w=True`,
52주 부트스트랩 지표가 저장된 일봉만으로 계산 가능함을 확인 → **M2.15C 메트릭 계산기 준비됨**. 5종목 execute-pilot은
여전히 미승인.

---

## 2. Approval scope (이 작업)

승인: 005930 **1종목만** · count=252 · target `market_data` timeframe `"1d"` · dev DB only · KIS read-only 일봉
경로 · 일봉 OHLCV insert/upsert · 문서 작성. 미승인: 5/20/110/전체 유니버스 · 마이그레이션 · 런타임 코드/프론트
변경 · SignalLog/Trade/Order · KIS 주문/place_order · 스케줄러/디스패처 · API 실행 엔드포인트 · M2.14B-3d ·
production 마이그레이션/DB. adjusted=False(원주가)만 사용(수정주가 채택은 추후).

---

## 3. Pre-execute safety checkpoint

| 항목 | 값 |
|------|-----|
| git status | clean |
| local main == origin/main | `5d83645` |
| alembic heads (code) | `q1r2s3t4u5v6` |
| alembic current (dev DB) | `q1r2s3t4u5v6` |
| pending/new migration | 없음 |
| KIS_REAL_TRADING_ENABLED | false |
| paper_signal_session_runner_enabled | false |
| paper_signal_recurring_plan_dispatcher_enabled | false |
| market_data `1d` total (before) | 0 |
| market_data `1d` 005930 (before) | 0 |
| market_data total (before) | 277,974 |
| SignalLog (before) | 95,110 |
| Trade (before) | 35 |
| Order | 별도 테이블 없음(SignalLog 전용 아키텍처) |

005930 일봉 0건 → 직전 파일럿 흔적 없음, 정상 진행.

---

## 4. Execute-path verification (실행 전 코드 점검)

`scripts/collect_daily_candles.py` + `services/market_data_daily_collector.py`:

1. script dry-run 기본.
2. execute에 `--execute` **및** `--confirm-daily-candle-collection` 둘 다 필요.
3. hard guard 활성(`evaluate_guards`: prod env / 누락 confirm·execute / 위험 플래그 true / non-local DB → 거부).
4. provider = KIS read-only quotations 클라이언트.
5. `get_daily_candles` TR `FHKST03010100`.
6. 주문 TR(VTTC0012U/0011U) 미사용.
7. `place_order` 미관여.
8. upsert 대상 `market_data` `timeframe="1d"`(`DAILY_TIMEFRAME`).
9. 기존 행 로드를 `timeframe=="1d"`로 한정 → **인트라데이 1m/5m 절대 미수정**.
10. 중복 방지: 동일값 skip(멱등) + repo `upsert_bulk` `ON CONFLICT(symbol,timeframe,ts)`.
11. trading_value는 DB 저장에 불필요(미저장).
12. SignalLog/Trade/Order 경로 없음(report에 0 고정).
13. 스케줄러/디스패처 경로 없음.

→ 안전. execute 진행.

---

## 5. Dry-run / coverage (pre-execute)

- dry-run(005930, count=252): **writes=0**, `estimated_requests=3`, 라이브 KIS 미호출.
- coverage-only(005930, pre): `daily_candles=0`, `ready_for_52w=False`, 읽기 전용.

---

## 6. Execute command / method

- 명령: `collect_daily_candles.py --symbols 005930 --count 252 --execute --confirm-daily-candle-collection --json`.
- 1회 실행만. provider가 `get_daily_candles("005930", count=252)`(adjusted 기본 False/원주가)로 페이지네이션
  수집 → collector가 멱등 upsert → 스크립트가 `session.commit()`.
- 시크릿 미출력(헤더 인증).

---

## 7. Execute result

`{"mode":"execute","symbols":["005930"],"count":252,"writes":252,"signals_created":0,"trades_created":0,`
`"orders_created":0,"per_symbol":[{"symbol_code":"005930","status":"success","fetched":252,"inserted":252,`
`"conflicts":0,"reason":null}]}`

- status=**success**, fetched=252, inserted=252, conflicts=0, writes=252. 신호/거래/주문 0.

---

## 8. Provider / page behavior

- M2.15B-5와 동일하게 3 페이지 호출로 252봉 수집(1회 ~100봉 한도).
- rate-limit: `EGW00201` 1회 관측 → `_request` 내장 backoff가 자동 재시도하여 복구(데이터 손실 없음).

---

## 9. DB before / after

| 지표 | before | after | 변화 | 해석 |
|------|:---:|:---:|:---:|------|
| market_data `1d` total | 0 | **252** | +252 | 005930 일봉 적재 |
| market_data `1d` 005930 | 0 | **252** | +252 | 본 파일럿 |
| market_data total | 277,974 | 278,279 | +305 | +252(본 작업) + ~53(무관 백그라운드 인트라데이) |
| SignalLog | 95,110 | 95,110 | **0** | 본 작업 신호 생성 0 |
| Trade | 35 | 35 | **0** | 거래 미생성 |

---

## 10. 005930 daily rows — count / date range

- 행 수: **252**.
- 거래일 범위(KST): **2025-06-18 ~ 2026-06-29**(M2.15B-5 프로브의 oldest 20250618 / newest 20260629와 일치).
- 비고: `ts`는 거래일의 **KST 자정** tz-aware 값. `ts::date`를 UTC 세션에서 캐스팅하면 D-1로 보이지만(표시 artifact),
  `ts at time zone 'Asia/Seoul'`로 보면 정확히 거래일과 일치. 저장 instant는 정확함.

---

## 11. Duplicate check

- `(symbol_code, timeframe, ts)` 중복 그룹: **0**. PK + `ON CONFLICT`로 보장.

---

## 12. OHLCV completeness

- open/high/low/close/volume **결측 0행**. 최신(2026-06-29 KST) 샘플: O 337000 / H 345000 / L 316000 /
  C 323000 / V 51,942,901 (프로브 최신봉과 정합).

---

## 13. Intraday preservation

- 005930 `1m` 행 = 2,274, `5m` 행 = 3,127 — 본 작업 전후 불변(execute는 `timeframe="1d"`만 대상,
  인트라데이 미수정).

---

## 14. Post-execute coverage

- coverage-only(005930, post): `symbols_with_daily=1`, `daily_candles=252`, `has_20/50/120/252=True`,
  `ready_for_52w=True`, `any_ready_for_52w=True`. coverage는 읽기 전용(쓰기 0).

---

## 15. 52-week metric sanity check (read-only, 미저장)

저장된 005930 일봉만으로 계산(영속화/SignalLog 생성 없음):

| 메트릭 | 값 |
|------|-----|
| current_close | 323,000 |
| high_52w | 380,000 |
| low_52w | 57,600 |
| low_52w_gain_pct | 460.76% |
| drawdown_from_52w_high_pct | 15.0% |
| MA20 | 335,800 |
| MA50 | 289,370 |
| recent_20d_high | 380,000 |
| volume_20d_avg | 60,912,053 |

→ **52주 부트스트랩 지표가 저장 일봉만으로 계산 가능**. 252행은 M2.15C 파일럿 메트릭 계산기에 충분. (값 자체는
KIS paper 응답 기준이며, 전략 조건 A/B 분류는 M2.15C에서 수행 — 본 작업은 계산 가능성만 확인.)

---

## 16. SignalLog / Trade / Order safety

- 본 작업 SignalLog 생성 **0**(95,110 불변), Trade **0**(35 불변), Order 테이블 없음/미생성.
- collector report의 signals/trades/orders 카운터 모두 0.

---

## 17. Scheduler / dispatcher safety

- 스케줄러/디스패처 미실행·미활성. 3 플래그(`KIS_REAL_TRADING_ENABLED` /
  `paper_signal_session_runner_enabled` / `paper_signal_recurring_plan_dispatcher_enabled`) false 유지.

---

## 18. Adjusted / raw & trading_value notes

- `adjusted=False`(원주가, `FID_ORG_ADJ_PRC=0`)로 적재 — 본 작업에서 수정주가로 전환하지 않음. 수정주가 채택은
  영속화 검증 이후 별도 결정.
- trading_value(`acml_tr_pbmn`)는 응답에 존재하나 V1 스키마에 컬럼이 없어 **미저장**. 컬럼 추가는 별도 승인
  마이그레이션.

---

## 19. Issue / fix needed

- 없음. execute 성공·멱등·중복 0·결측 0·인트라데이 보존·안전 불변식 유지. (관측된 `EGW00201` 1회는 내장
  backoff로 정상 복구.)

---

## 20. Next recommendation

- **성공** → 다음 후보(별도 승인): **M2.15B-7 — (a) 멱등성/포스트-영속 스모크(동일 005930 재-execute로 inserted=0
  / skipped_fresh 확인), 또는 (b) 5종목 execute-pilot 계획.** 우선 멱등성 스모크를 권장(영속 검증 마무리 후 다종목).
- 5종목 execute-pilot은 **미승인**.

---

## 21. 5-symbol execute-pilot 미승인 명시

**5종목(및 그 이상) execute-pilot은 이 작업으로 승인되지 않았다.** 본 작업은 005930 단일 종목 적재만 수행했다.
M2.14B-3d(무인 디스패처)도 별개이며 미승인 유지.
