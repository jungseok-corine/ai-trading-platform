# M2.15E — Leader Trend Research-Only Frontend UI

> 기존 read-only 백엔드 API(`GET /api/v1/leader-trend/candidates`)만 소비하는 **읽기 전용 프론트 섹션**. **백엔드
> 런타임 변경 0 · 신규 엔드포인트 0 · 후보 영속화 0 · CandidateEvent 0 · SignalLog/Trade/Order 0 · 주문/스케줄러 0 ·
> 라이브 KIS 0 · 일봉 fetch 0 · DB write 0 · 마이그레이션 0 · `KIS_REAL_TRADING_ENABLED` 미변경 · M2.14B-3d 미승인.**
> **후보는 매수 신호가 아니다.** 주문/진입/거래/추천 액션 버튼 없음(GET only).

---

## 1. Executive summary

`연구` 탭에 **"주도주 리서치"** 섹션 추가 — pilot_5 후보를 표로 노출하고 상단에 **매수 신호 아님 + 출처/한계 경고**를
강조. 000660·005930를 연구 후보(B, 고변동)로, 035420/005380/051910을 "해당 없음"으로 표시. **읽기 전용 GET**만
사용, 행동 버튼·POST/PUT/PATCH/DELETE 없음. 프론트 `npm run build`(tsc --noEmit + vite) 통과. 백엔드/DB/안전
불변식 무변경.

---

## 2. UI scope

`frontend/` 한정. `연구(Research)` 탭 내 신규 섹션(`주도주 리서치`). 한 섹션 = 경고 배너 + 결과 표 + 후보별 경고
상세. loading/error/empty 상태 처리. **버튼/액션 없음**(TanStack Query 마운트 시 자동 GET).

---

## 3. Route/page added

- 신규 컴포넌트 `frontend/src/components/research/LeaderTrendSection.tsx`.
- `ResearchPage.tsx`에 Section 키 `leader-trend`(라벨 "주도주 리서치") 추가 + 렌더 분기.
- API 함수 `getLeaderTrendCandidates`(`src/api/research.ts`) + 타입 `LeaderTrendCandidatesResponse`/
  `LeaderTrendCandidateResult`(`src/types/research.ts`). **백엔드 변경 없음.**

---

## 4. API endpoint consumed

`GET /api/v1/leader-trend/candidates` (read-only). POST/PUT/PATCH/DELETE 미사용. 브로커/KIS 직접 호출 없음.

---

## 5. Warning / provenance behavior

- 상단 적색 배너: "⚠️ 매수 신호가 아닙니다(NOT buy signals). 연구용 필터일 뿐 주문·진입·거래·추천을 의미하지 않음."
- 백엔드 `safety_warning`·`provenance_warning`을 **그대로** 표시(약화 없음): "KIS 시세로 검증(실전 도메인 일봉) ·
  비-KIS 독립성 미확증".
- 메타: 범위(pilot_5) · 스캔 수 · 연구 후보 수 · research-only.

---

## 6. Candidate / non-candidate display

표 컬럼: 종목 · 분류 · 현재가 · 52주고 · 52주저 · 저점대비% · 고점낙폭% · MA20 · MA50 · 일봉수 · 비고(고변동) ·
운영분류. 후보별 상세 카드: 전략-극단 경고 · 조정 의심 경고 · 데이터 이상 · 기간 · 52주 준비.

| 종목 | 분류 표기 |
|------|------|
| 000660 | 후보 B(고변동, 매수 신호 아님) |
| 005930 | 후보 B(고변동, 매수 신호 아님) |
| 035420/005380/051910 | 해당 없음 |

분류 라벨은 "후보 A/B" 등 중립어만 사용 — buy/entry/order/trade/recommendation/매수/진입/추천/주문/거래 신호를
**액션 라벨로 쓰지 않음**(부정·경고 문구에서만).

---

## 7. Forbidden action verification

- 컴포넌트에 `<button>`·`onClick` **없음**(읽기 전용 표시 only).
- API 함수는 `apiClient.get`만. POST/PUT/PATCH/DELETE **없음**.
- buy/sell/enter/trade/order/create-signal/create-proposal/persist/add-to-strategy/auto-run/dispatch/scheduler
  버튼 **부재**.

---

## 8. Tests / typecheck / build

- 프론트 테스트 러너 미구성(package.json scripts에 test 없음) → 프로젝트 관례상 프론트 단위 테스트 미작성.
- **`npm run build`(tsc -p --noEmit + vite build) 통과** — 207 modules, 타입 에러 0.

---

## 9. Local verification

- API 계약 read-only 확인(백엔드 미변경): scope=pilot_5 · scanned=5 · operational_candidates=2 ·
  000660/005930 operational B(extreme) · 035420/005380/051910 none. UI는 이 응답을 그대로 렌더.
- 라이브 KIS 0 · DB write 0(엔드포인트/스캐너 read-only).

---

## 10. DB unchanged verification

| 지표 | before | after |
|------|:-:|:-:|
| market_data `1d` total | 1,260 | **1,260** |
| 005930 체크섬 | `3789bed6…` | `3789bed6…`(불변) |
| Trade | 35 | 35 |

---

## 11~12. SignalLog/Trade/Order & scheduler safety

본 작업 SignalLog 0 · Trade 0(35 불변) · Order 0 · CandidateEvent 0 · broker/주문 API 0. 스케줄러/디스패처
미실행·미활성 · 3 플래그 false(`KIS_REAL_TRADING_ENABLED` 포함).

---

## 13. Migration status

마이그레이션 0 · Alembic head `q1r2s3t4u5v6` 불변 · market_data 스키마 불변 · 후보 테이블 없음.

---

## 14. Safety search

변경은 frontend+docs only(`backend/` 변경 0). 신규 컴포넌트/API에 place_order/broker/KIS provider/POST·PUT·PATCH·
DELETE/mutation 없음. 시크릿 없음. buy/order/trade/추천 등은 경고/비목표 문구에만.

---

## 15. DECISIONS.md

미갱신. 본 UI는 D-29(read-only 후보 노출 정책)의 **얇은 소비자**일 뿐 새 내구 정책을 만들지 않는다(읽기 전용 ·
매수 신호 아님 · 무영속 · 무액션). 정책은 D-29가 이미 규정.

---

## 16~18. 명시

- **후보(A/B/none)는 매수 신호도 주문 신호도 아니다.** · **후보를 영속화하지 않았다.** · **주문/신호/거래를
  생성하지 않았다.** M2.14B-3d도 별개이며 미승인 유지.

---

## 19. Proposed next task

- **M2.15D-3C(선택, 별도 승인)**: 비-KIS 독립 소스(예 pykrx) 통합으로 실세계 정확성 최종 확증.
- 또는 후보 메타 영속화(CandidateEvent 재사용 — 승인 없는 자동 제안 없음·주문 없음)·종목 확장은 **별도 승인** 필요.
